تعديل جدول الطلبيات في بيان حساب العميل: استبدال أعمدة (سعر الشحن/تكلفة الشحنة/الإجمالي) بعمودي (القيمة المستلمة + ملاحظات)

- عمود "القيمة المستلمة" = إجمالي سعر الشحنة - تكلفة الشحن (courier cost)
- عمود "ملاحظات" حقل نصي قابل للتعديل مباشرة من الجدول، بيتحفظ
  تلقائيًا عند الخروج من الحقل (onBlur) عبر نفس PATCH endpoint
  الموجود (clientAccountManifestsApi.updateItem)، من غير ما يغيّر
  حالة التسليم الحالية
- إضافة كومبوننت NotesCell لإدارة الحفظ والـ state المحلي
- تعديل الـ grid template للجدول (Header + Row) ليطابق 8 أعمدة
  بدل 9
